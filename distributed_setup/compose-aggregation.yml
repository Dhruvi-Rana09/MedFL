version: "3.9"

# ═══════════════════════════════════════════════════════════════════════════
#  Device 3: Aggregation Node (Compute Plane)
# ═══════════════════════════════════════════════════════════════════════════

services:
  aggregation:
    build:
      context: ../services/aggregation
      dockerfile: Dockerfile
    ports:
      - "8003:8003"
    environment:
      PORT: "8003"  # Bind on 8003 to match medfl.env AGG_URL
    network_mode: "host"
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8003/health').raise_for_status()"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    restart: unless-stopped
